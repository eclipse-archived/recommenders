{
   "_id": "_design/compilationunits",
   "language": "javascript",
   "views": {
       "bySuperclass": {
           "map": "function(doc) {\n    if(!doc.imports || !doc.primaryType)\n        return;\n    var key = doc.primaryType.superclass;\n    emit(key, null);\n}",
           "reduce": "function (key, values, rereduce) {\n\n}"
       }
   }
}